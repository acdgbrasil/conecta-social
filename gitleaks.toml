title = "Conecta Social Secret Scan Defaults"

[allowlist]
description = "Ignorar diretórios gerados automaticamente"
paths = [
  "node_modules",
  "dist",
  "build",
  ".git",
  "reports",
  "deployments",
  "legacy"
]
