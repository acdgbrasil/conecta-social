meta {
  name: Download
  type: http
  seq: 1
}

post {
  url: {{baseURL}}/download
  body: none
  auth: none
}

tests {
  expect(res.getStatus()).to.equal(200);
  expect(res.getBody()).to.equal('Download endpoint hit');
}
