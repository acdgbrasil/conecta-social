meta {
  name: Create First Entry Observation
  type: http
  seq: 2
}

post {
  url: {{baseURL}}/create/first/entry/observation
  body: json
  auth: none
}

body:json {
  {
    "observation": "Some observation text",
    "whoIsObservingId": 1,
    "firstEntryInUnityId": 1
  }
}

tests {
  expect(res.getStatus()).to.equal(201);
}
