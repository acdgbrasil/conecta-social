meta {
  name: Create Family Composition Observation
  type: http
  seq: 4
}

post {
  url: {{baseURL}}/create/family/composition/observation
  body: json
  auth: none
}

body:json {
  {
    "observation": "Some observation text",
    "whoIsObservingId": 1,
    "familyCompositionID": 1
  }
}

tests {
  expect(res.getStatus()).to.equal(201);
}
