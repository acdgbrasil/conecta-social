meta {
  name: Create Family Community Observation
  type: http
  seq: 2
}

post {
  url: {{baseURL}}/create/family/community/observation
  body: json
  auth: none
}

body:json {
  {
    "observation": "Some observation text",
    "whoIsObservingId": 1,
    "familyAndCommunityId": 1
  }
}

tests {
  expect(res.getStatus()).to.equal(201);
}
