meta {
  name: Create Reference Person Observation
  type: http
  seq: 4
}

post {
  url: {{baseURL}}/create/reference/person/observation
  body: json
  auth: none
}

body:json {
  {
    "observation": "Some observation text",
    "whoIsObservingId": 1,
    "referencePersonId": 1
  }
}

tests {
  expect(res.getStatus()).to.equal(201);
}
